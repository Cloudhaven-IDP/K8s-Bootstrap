operatorConfig:
  hostname: humboldt-operator
  defaultTags:
    - tag:k8s-worker

proxyConfig:
  defaultTags: "tag:k8s-worker"

# oauth left empty — ExternalSecret creates the operator-oauth secret
# from SSM before the operator starts
oauth:
  clientId: ""
  clientSecret: ""
